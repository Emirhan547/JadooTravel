@model List<ResultTripPlanDto>

@{
    ViewData["Title"] = "Tur Planları";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<div class="row">
    <div class="col-12">

        <div class="card card-outline card-primary">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fa fa-route mr-1"></i> Tur Planı Listesi
                </h3>

                <div class="card-tools">
                    <a href="/Admin/TripPlan/CreateTripPlan" class="btn btn-info btn-sm">
                        <i class="fa fa-plus"></i> Yeni Tur Planı
                    </a>
                </div>
            </div>

            <div class="card-body p-0">
                <table class="table table-bordered table-hover mb-0">
                    <thead class="thead-light">
                        <tr>
                            <th style="width:50px">#</th>
                            <th>Başlık</th>
                            <th>Açıklama</th>
                            <th>İkon</th>
                            <th style="width:160px">İşlemler</th>
                        </tr>
                    </thead>

                    <tbody>
                        @if (Model != null && Model.Count > 0)
                        {
                            foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td>@item.Title</td>
                                    <td>@item.Description</td>
                                    <td>
                                        <i class="@item.IconUrl"></i>
                                        <small class="text-muted ml-1">@item.IconUrl</small>
                                    </td>
                                    <td>
                                        <a href="/Admin/TripPlan/UpdateTripPlan/@item.Id"
                                           class="btn btn-success btn-sm">
                                            <i class="fa fa-edit"></i>
                                        </a>

                                        <a href="/Admin/TripPlan/DeleteTripPlan/@item.Id"
                                           class="btn btn-danger btn-sm"
                                           onclick="return confirm('Bu tur planını silmek istediğinize emin misiniz?')">
                                            <i class="fa fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" class="text-center text-muted p-4">
                                    Kayıtlı tur planı bulunamadı.
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>

        </div>

    </div>
</div>
